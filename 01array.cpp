#include <bits/stdc++.h>
using namespace std;

int main(){
    // int arr[10];
    
    // 静态数组定义，赋值
    // memset(arr, 0, sizeof(arr));
    // arr[0] = 1;
    // arr[1] = 2;
    // int a = arr[0];
    // cout << a << endl;

    // 增-尾加
    // for(int i = 0; i < 10; i++){
    //     arr[i] = i;
    // }
    // arr[4] = 4;
    // arr[5] = 5;

    // 增-插入
    // for(int i = 4; i > 2; i--){
    //     arr[i] = arr[i-1];
    // }
    // arr[2] = 666;

    // 增-内存空间满
    // int newArr[20];
    // memset(newArr, 0, sizeof(newArr));
    // for(int i = 0; i < 10; i++){
    //     newArr[i] = arr[i];
    // }
    // newArr[10] = 666;

    // newArr[10] = -1;    // -1 means delete

    // 删
    // for(int i = 0; i < 5; i++){
    //     arr[i] = i;
    // }

    // for(int i = 1; i < 5; i++){
    //     arr[i] = arr[i+1];
    // }
    // arr[4] = -1;
    
    return 0;
    
}
